package main.util;

import TestBase.WebTestBase;

public class IframeUtil extends WebTestBase
{
        /*public static void iframeFunctions()
            {


                List<WebElement> allFrame =driver.findElements(By.tagName("iframe"));
                System.out.println(allFrame.size());

                //driver.switchTo().frame(1);

                //driver.switchTo().frame("frame1");

                driver.switchTo().frame(driver.findElement(By.xpath("//iframe[@id='frame1']")));

                WebElement print=driver.findElement(By.xpath("//h1[text()='This is a sample page']"));
                System.out.println(print.getText());

                //iframe[@name='gform_ajax_frame_8']

        }
    }*/
}
